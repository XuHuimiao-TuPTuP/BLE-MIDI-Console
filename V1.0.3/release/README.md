# BLE MIDI 调试器 V1.0.3 Release

请从 GitHub Release 下载 `BLE-MIDI-Debugger-V1.0.3-Windows-x64.zip`，完整解压后运行目录内的 `BLE-MIDI-Debugger-V1.0.3.exe`。

V1.0.3 改用非自解压的目录构建，避免 PyInstaller 单文件自解压行为引发杀毒软件误报。请勿只复制 EXE；程序运行需要同目录中的 `_internal` 文件夹。

可使用 `SHA256SUMS.txt` 核对 ZIP 文件完整性。
